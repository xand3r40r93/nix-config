{ inputs, ... }:
{

  # aerospace = import ./packages/aerospace.nix;

  # unstable-packages = final: _prev: {
  #   unstable = import inputs.nixpkgs {
  #     system = final.system;
  #     config.allowUnfree = true;
  #   };
  # };

}
